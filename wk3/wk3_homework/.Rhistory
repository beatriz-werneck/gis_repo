max_temp_bra <- terra::extract(max_temp_world, sites)
View(max_temp_bra)
View(sites)
#add the city names to the rows
max_temp_bra_2 <- max_temp_bra %>%
as_tibble() %>%
add_column(Site = rownames(sites), .before = "SSP_126_Jan")
#add the city names to the rows
sites
#add the city names to the rows
max_temp_bra_2 <- max_temp_bra %>%
as_tibble() %>%
add_column(Site = key_cities_bra$CITY_NAME, .before = "SSP_126_Jan")
View(max_temp_bra_2)
max_temp_world$SSP_126_Jan
View(max_temp_world)
View(max_temp_bra)
View(max_temp_bra)
View(max_temp_bra_2)
View(max_temp_bra)
#import libraries
library(dplyr)
library(fs)
library(here)
library(raster)
library(sf)
library(stringr)
library(terra)
library(tidyverse)
#VECTOR DATA
#check geopackage layers
st_layers(here("wk3_hw_data", "gadm41_BRA.gpkg"))
#read geopackage layer for brazil outline
bra_outline <- st_read(here("wk3_hw_data", "gadm41_BRA.gpkg"),
layer = 'ADM_ADM_0')
#simplify brazil outline
bra_outline_simple <- bra_outline$geom %>%
st_simplify(.,dTolerance = 1000)
#CSV DATA
#read csv with coordinates from Brazilian key cities
key_cities_world <- read.csv(here::here("wk3_hw_data/", "World_Cities.csv"),
header = TRUE,
sep = ",",
encoding = "latin1",
na = "n/a")
key_cities_bra <- key_cities_world %>%
filter(str_detect(key_cities_world$FIPS_CNTRY, 'BR'))
sites_sf <- st_as_sf(key_cities_bra, coords = c('X', 'Y'), crs = 4326)
View(sites_sf)
#import libraries
library(dplyr)
library(fs)
library(here)
library(raster)
library(sf)
library(stringr)
library(terra)
library(tidyverse)
#VECTOR DATA
#check geopackage layers
st_layers(here("wk3_hw_data", "gadm41_BRA.gpkg"))
#read geopackage layer for brazil outline
bra_outline <- st_read(here("wk3_hw_data", "gadm41_BRA.gpkg"),
layer = 'ADM_ADM_0')
#simplify brazil outline
bra_outline_simple <- bra_outline$geom %>%
st_simplify(.,dTolerance = 1000)
#CSV DATA
#read csv with coordinates from Brazilian key cities
key_cities_world <- read.csv(here::here("wk3_hw_data/", "World_Cities.csv"),
header = TRUE,
sep = ",",
encoding = "latin1",
na = "n/a")
key_cities_bra <- key_cities_world %>%
filter(str_detect(key_cities_world$FIPS_CNTRY, 'BR'))
#sites <- key_cities_bra[,c(1,2)]
#rownames(sites) <- key_cities_bra$CITY_NAME
#RASTER DATA
#load all of the data into a SpatRaster (collection of raster layers with the same spatial extent and resolution)
tif_files <- dir_info("wk3_hw_data/") %>%
filter(str_detect(path, ".tif")) %>%
dplyr::select(path) %>%
pull()
max_temp_world <- tif_files %>%
terra::rast()
#organize the SpatRaster layer names
month <- c("Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec")
ssp <- c("SSP_126", "SSP_245", "SSP_370", "SSP_585")
layer_combinations <- expand.grid(Month = month, SSP = ssp)
layer_names <- paste(layer_combinations$SSP, layer_combinations$Month, sep = "_")
names(max_temp_world) <- layer_names
#certify that there isn't a mismatch between coordinate systems
sites_sf <- st_as_sf(key_cities_bra, coords = c('X', 'Y'), crs = 4326)
sites_transformed <- st_transform(sites_sf, crs(max_temp_world))
View(sites_transformed)
sites_vect <- terra::vect(sites_transformed)
View(sites_vect)
#extract the data form the SparRaster for all the brazilian key cities
max_temp_bra <- terra::extract(max_temp_world, sites_vect)
View(max_temp_bra)
#add the city names to the rows
max_temp_bra_2 <- max_temp_bra %>%
as_tibble() %>%
add_column(Site = key_cities_bra$CITY_NAME, .before = "SSP_126_Jan")
View(max_temp_bra_2)
#import libraries
library(dplyr)
library(fs)
library(here)
library(raster)
library(sf)
library(stringr)
library(terra)
library(tidyverse)
#VECTOR DATA
#check geopackage layers
st_layers(here("wk3_hw_data", "gadm41_BRA.gpkg"))
#read geopackage layer for brazil outline
bra_outline <- st_read(here("wk3_hw_data", "gadm41_BRA.gpkg"),
layer = 'ADM_ADM_0')
#simplify brazil outline
bra_outline_simple <- bra_outline$geom %>%
st_simplify(.,dTolerance = 1000)
#CSV DATA
#read csv with coordinates from Brazilian key cities
key_cities_world <- read.csv(here::here("wk3_hw_data/", "World_Cities.csv"),
header = TRUE,
sep = ",",
encoding = "latin1",
na = "n/a")
key_cities_bra <- key_cities_world %>%
filter(str_detect(key_cities_world$FIPS_CNTRY, 'BR'))
sites <- key_cities_bra[,c(1,2)]
rownames(sites) <- key_cities_bra$CITY_NAME
#RASTER DATA
#load all of the data into a SpatRaster (collection of raster layers with the same spatial extent and resolution)
tif_files <- dir_info("wk3_hw_data/") %>%
filter(str_detect(path, ".tif")) %>%
dplyr::select(path) %>%
pull()
max_temp_world <- tif_files %>%
terra::rast()
#organize the SpatRaster layer names
month <- c("Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec")
ssp <- c("SSP_126", "SSP_245", "SSP_370", "SSP_585")
layer_combinations <- expand.grid(Month = month, SSP = ssp)
layer_names <- paste(layer_combinations$SSP, layer_combinations$Month, sep = "_")
names(max_temp_world) <- layer_names
#extract the data form the SparRaster for all the brazilian key cities
max_temp_bra <- terra::extract(max_temp_world, sites)
#add the city names to the rows
max_temp_bra_2 <- max_temp_bra %>%
as_tibble() %>%
add_column(Site = key_cities_bra$CITY_NAME, .before = "SSP_126_Jan")
View(max_temp_bra_2)
View(key_cities_bra)
View(sites)
View(key_cities_world)
View(key_cities_bra)
bra_outline_simple
st_crs(bra_outline)$proj4string
st_crs(bra_outline_simple)$proj4string
#reproject data for local project CRS
bra_outline_prj <- bra_outline %>%
st_transform(.,5641)
print(bra_outline_prj)
print(max_temp_world)
#reproject data for the local projected CRS
max_temp_world_prj <- terra::project(max_temp_world, "+proj=merc +lat_ts=-2 +lon_0=-43 +x_0=5000000 +y_0=10000000 +ellps=GRS80 +towgs84=0,0,0,0,0,0,0 +units=m +no_defs +type=crs")
print(max_temp_world_prj)
plot(max_temp_world_prj)
names(max_temp_world_prj) <- layer_names
#extract the data form the SparRaster for all the brazilian key cities
max_temp_bra <- terra::extract(max_temp_world_prj, sites)
#add the city names to the rows
max_temp_bra_2 <- max_temp_bra %>%
as_tibble() %>%
add_column(Site = key_cities_bra$CITY_NAME, .before = "SSP_126_Jan")
View(max_temp_bra_2)
View(max_temp_world)
View(max_temp_world_prj)
View(sites)
View(key_cities_bra)
View(key_cities_world)
View(max_temp_bra)
#import libraries
library(dplyr)
library(fs)
library(here)
library(raster)
library(sf)
library(stringr)
library(terra)
library(tidyverse)
#VECTOR DATA
#check geopackage layers
st_layers(here("wk3_hw_data", "gadm41_BRA.gpkg"))
#read geopackage layer for brazil outline
bra_outline <- st_read(here("wk3_hw_data", "gadm41_BRA.gpkg"),
layer = 'ADM_ADM_0')
#reproject data for local projected CRS
bra_outline_prj <- bra_outline %>%
st_transform(.,5641)
print(bra_outline_prj)
#simplify brazil outline
bra_outline_simple <- bra_outline_prj$geom %>%
st_simplify(.,dTolerance = 1000)
View(bra_outline_simple)
plot(bra_outline_simple)
print(bra_outline_simple)
#CSV DATA
#read csv with coordinates from Brazilian key cities
key_cities_world <- read.csv(here::here("wk3_hw_data/", "World_Cities.csv"),
header = TRUE,
sep = ",",
encoding = "latin1",
na = "n/a")
View(key_cities_world)
key_cities_bra <- key_cities_world %>%
filter(str_detect(key_cities_world$FIPS_CNTRY, 'BR'))
View(key_cities_bra)
sites <- key_cities_bra[,c(1,2)]
rownames(sites) <- key_cities_bra$CITY_NAME
View(sites)
#RASTER DATA
#load all of the data into a SpatRaster (collection of raster layers with the same spatial extent and resolution)
tif_files <- dir_info("wk3_hw_data/") %>%
filter(str_detect(path, ".tif")) %>%
dplyr::select(path) %>%
pull()
max_temp_world <- tif_files %>%
terra::rast()
print(max_temp_world)
#reproject data for the local projected CRS
max_temp_world_prj <- terra::project(max_temp_world, "+proj=merc +lat_ts=-2 +lon_0=-43 +x_0=5000000 +y_0=10000000 +ellps=GRS80 +towgs84=0,0,0,0,0,0,0 +units=m +no_defs +type=crs")
print(max_temp_world_prj)
plot(max_temp_world_prj)
print(max_temp_world_prj$tmax01)
print(max_temp_world_prj[1,1])
print(max_temp_world[1,1])
print(max_temp_world_prj)
print(max_temp_world_prj[1,2])
print(max_temp_world_prj[[2,1]])
print(max_temp_world_prj[[1]])
print(max_temp_world_prj[[,1]])
print(max_temp_world_prj[[2]])
print(max_temp_world_prj[[]])
print(max_temp_world_prj[[5]])
print(max_temp_world_prj[[1:12]])
plot(max_temp_world_prj[[1:12]])
print(max_temp_world_prj[[1]])
plot(max_temp_world_prj[[1]])
print(max_temp_world_prj[[1:12]])
plot(max_temp_world_prj[[1:12]])
#organize the SpatRaster layer names
month <- c("Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec")
ssp <- c("SSP_126", "SSP_245", "SSP_370", "SSP_585")
layer_combinations <- expand.grid(Month = month, SSP = ssp)
layer_names <- paste(layer_combinations$SSP, layer_combinations$Month, sep = "_")
names(max_temp_world_prj) <- layer_names
print(max_temp_world_prj[[1:12]])
plot(max_temp_world_prj[[1:12]])
#extract the data form the SparRaster for all the brazilian key cities
max_temp_bra <- terra::extract(max_temp_world_prj, sites)
View(max_temp_bra)
View(max_temp_bra)
View(max_temp_world_prj)
print(max_temp_world_prj[[1:12]])
print(max_temp_world_prj[[1]])
typeof(max_temp_world_prj[[1]])
max_temperatures <- global(max_temp_world_prj, fun = "max", na.rm = TRUE)
View(max_temperatures)
#import libraries
library(dplyr)
library(fs)
library(here)
library(raster)
library(sf)
library(stringr)
library(terra)
library(tidyverse)
#VECTOR DATA
#check geopackage layers
st_layers(here("wk3_hw_data", "gadm41_BRA.gpkg"))
#read geopackage layer for brazil outline
bra_outline <- st_read(here("wk3_hw_data", "gadm41_BRA.gpkg"),
layer = 'ADM_ADM_0')
#reproject data for local projected CRS
bra_outline_prj <- bra_outline %>%
st_transform(.,5641)
print(bra_outline_prj)
#simplify brazil outline
bra_outline_simple <- bra_outline_prj$geom %>%
st_simplify(.,dTolerance = 1000)
plot(bra_outline_simple)
print(bra_outline_simple)
#CSV DATA
#read csv with coordinates from Brazilian key cities
key_cities_world <- read.csv(here::here("wk3_hw_data/", "World_Cities.csv"),
header = TRUE,
sep = ",",
encoding = "latin1",
na = "n/a")
key_cities_bra <- key_cities_world %>%
filter(str_detect(key_cities_world$FIPS_CNTRY, 'BR'))
sites <- key_cities_bra[,c(1,2)]
rownames(sites) <- key_cities_bra$CITY_NAME
#RASTER DATA
#load all of the data into a SpatRaster (collection of raster layers with the same spatial extent and resolution)
tif_files <- dir_info("wk3_hw_data/") %>%
filter(str_detect(path, ".tif")) %>%
dplyr::select(path) %>%
pull()
max_temp_world <- tif_files %>%
terra::rast()
print(max_temp_world)
#reproject data for the local projected CRS
max_temp_world_prj <- terra::project(max_temp_world, "+proj=merc +lat_ts=-2 +lon_0=-43 +x_0=5000000 +y_0=10000000 +ellps=GRS80 +towgs84=0,0,0,0,0,0,0 +units=m +no_defs +type=crs")
print(max_temp_world_prj[[1:12]])
plot(max_temp_world_prj[[1:12]])
max_temp_bra <- terra::extract(max_temp_world_prj, bra_outline_simple)
#organize the SpatRaster layer names
month <- c("Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec")
ssp <- c("SSP_126", "SSP_245", "SSP_370", "SSP_585")
layer_combinations <- expand.grid(Month = month, SSP = ssp)
layer_names <- paste(layer_combinations$SSP, layer_combinations$Month, sep = "_")
names(max_temp_world_prj) <- layer_names
print(max_temp_world_prj)
plot(max_temp_world_prj)
max_temperatures <- global(max_temp_world_prj, fun = "max", na.rm = TRUE)
View(max_temperatures)
#extract the data form the SparRaster for all the brazilian key cities
max_temp_bra <- terra::extract(max_temperatures, sites)
View(max_temperatures)
View(sites)
#convert to spatial vector
city_points <- vect(sites, geom = c("X", "Y"), crs = "EPSG:5641")
View(city_points)
city_points
max_temp_world_prj
#extract the data form the SparRaster for all the brazilian key cities
max_temp_bra <- terra::extract(max_temp_world_prj, city_points)
View(max_temp_bra)
#import libraries
library(dplyr)
library(fs)
library(here)
library(raster)
library(sf)
library(stringr)
library(terra)
library(tidyverse)
#VECTOR DATA
#check geopackage layers
st_layers(here("wk3_hw_data", "gadm41_BRA.gpkg"))
#read geopackage layer for brazil outline
bra_outline <- st_read(here("wk3_hw_data", "gadm41_BRA.gpkg"),
layer = 'ADM_ADM_0')
#reproject data for local projected CRS
bra_outline_prj <- bra_outline %>%
st_transform(.,5641)
print(bra_outline_prj)
#simplify brazil outline
bra_outline_simple <- bra_outline_prj$geom %>%
st_simplify(.,dTolerance = 1000)
plot(bra_outline_simple)
print(bra_outline_simple)
#CSV DATA
#read csv with coordinates from Brazilian key cities
key_cities_world <- read.csv(here::here("wk3_hw_data/", "World_Cities.csv"),
header = TRUE,
sep = ",",
encoding = "latin1",
na = "n/a")
key_cities_bra <- key_cities_world %>%
filter(str_detect(key_cities_world$FIPS_CNTRY, 'BR'))
sites <- key_cities_bra[,c(1,2)]
rownames(sites) <- key_cities_bra$CITY_NAME
#convert to spatial vector
city_points <- vect(sites, geom = c("X", "Y"), crs = "EPSG:5641")
city_points
#RASTER DATA
#load all of the data into a SpatRaster (collection of raster layers with the same spatial extent and resolution)
tif_files <- dir_info("wk3_hw_data/") %>%
filter(str_detect(path, ".tif")) %>%
dplyr::select(path) %>%
pull()
max_temp_world <- tif_files %>%
terra::rast()
print(max_temp_world)
#reproject data for the local projected CRS
max_temp_world_prj <- terra::project(max_temp_world, "EPSG:5641")
max_temp_world_prj
#organize the SpatRaster layer names
month <- c("Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec")
ssp <- c("SSP_126", "SSP_245", "SSP_370", "SSP_585")
layer_combinations <- expand.grid(Month = month, SSP = ssp)
layer_names <- paste(layer_combinations$SSP, layer_combinations$Month, sep = "_")
names(max_temp_world_prj) <- layer_names
print(max_temp_world_prj)
plot(max_temp_world_prj)
#extract the data form the SparRaster for all the brazilian key cities
max_temp_bra <- terra::extract(max_temp_world_prj, city_points)
View(max_temp_bra)
#add the city names to the rows
max_temp_bra_2 <- max_temp_bra %>%
as_tibble() %>%
add_column(Site = key_cities_bra$CITY_NAME, .before = "SSP_126_Jan")
View(max_temp_bra_2)
View(max_temp_bra)
bra_temp <- bra_outline_simple %>%
terra::crop(max_temp_world_prj, .)
plot(bra_temp)
exact_bra <- terra::mask(bra_temp, bra_outline_simple)
plot(exact_bra)
exact_bra <- terra::mask(bra_temp, bra_outline_simple)
exact_bra <- terra::mask(bra_temp, bra_outline)
plot(exact_bra)
View(exact_bra)
#import libraries
library(dplyr)
library(fs)
library(here)
library(raster)
library(sf)
library(stringr)
library(terra)
library(tidyverse)
#VECTOR DATA
#check geopackage layers
st_layers(here("wk3_hw_data", "gadm41_BRA.gpkg"))
#read geopackage layer for brazil outline
bra_outline <- st_read(here("wk3_hw_data", "gadm41_BRA.gpkg"),
layer = 'ADM_ADM_0')
#reproject data for local projected CRS
bra_outline_prj <- bra_outline %>%
st_transform(.,5641)
print(bra_outline_prj)
#simplify brazil outline
bra_outline_simple <- bra_outline_prj$geom %>%
st_simplify(.,dTolerance = 1000)
plot(bra_outline_simple)
print(bra_outline_simple)
#CSV DATA
#read csv with coordinates from Brazilian key cities
key_cities_world <- read.csv(here::here("wk3_hw_data/", "World_Cities.csv"),
header = TRUE,
sep = ",",
encoding = "latin1",
na = "n/a")
key_cities_bra <- key_cities_world %>%
filter(str_detect(key_cities_world$FIPS_CNTRY, 'BR'))
sites <- key_cities_bra[,c(1,2)]
rownames(sites) <- key_cities_bra$CITY_NAME
#convert to spatial vector
city_points <- vect(sites, geom = c("X", "Y"), crs = "EPSG:5641")
city_points
#RASTER DATA
#load all of the data into a SpatRaster (collection of raster layers with the same spatial extent and resolution)
tif_files <- dir_info("wk3_hw_data/") %>%
filter(str_detect(path, ".tif")) %>%
dplyr::select(path) %>%
pull()
max_temp_world <- tif_files %>%
terra::rast()
print(max_temp_world)
#reproject data for the local projected CRS
max_temp_world_prj <- terra::project(max_temp_world, "EPSG:5641")
max_temp_world_prj
#organize the SpatRaster layer names
month <- c("Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec")
ssp <- c("SSP_126", "SSP_245", "SSP_370", "SSP_585")
layer_combinations <- expand.grid(Month = month, SSP = ssp)
layer_names <- paste(layer_combinations$SSP, layer_combinations$Month, sep = "_")
names(max_temp_world_prj) <- layer_names
print(max_temp_world_prj)
plot(max_temp_world_prj)
#extract the data form the SparRaster for all the brazilian key cities
max_temp_bra <- terra::extract(max_temp_world_prj, city_points)
#add the city names to the rows
max_temp_bra_2 <- max_temp_bra %>%
as_tibble() %>%
add_column(Site = key_cities_bra$CITY_NAME, .before = "SSP_126_Jan")
bra_temp <- bra_outline_simple %>%
terra::crop(max_temp_world_prj, .)
plot(bra_temp)
exact_bra <- terra::mask(bra_temp, bra_outline)
plot(exact_bra)
#import libraries
library(dplyr)
library(fs)
library(here)
library(raster)
library(sf)
library(stringr)
library(terra)
library(tidyverse)
#VECTOR DATA
#check geopackage layers
st_layers(here("wk3_hw_data", "gadm41_BRA.gpkg"))
View(key_cities_bra)
View(max_temp_world)
View(max_temp_bra_2)
View(max_temp_bra_2)
View(max_temp_bra)
View(max_temp_bra_2)
View(max_temp_world)
View(max_temp_world_prj)
View(max_temp_bra)
View(max_temp_bra_2)
View(max_temp_world)
